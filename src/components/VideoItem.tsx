type VideoItemsProps = {
        user_id: "string",
        description: "string",
        video_url: "string",
        title: "string"
}

export function VideoItems ({
    user_id, 
    description,
    video_url,
    title,
}: VideoItemsProps) {

    return <div>
        Hello
    </div>
}